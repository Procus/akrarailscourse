  <strong>
    <%= comment.commenter %>
  </strong>
 
  <p>
    <%= comment.body %>
  </p>
  
  <% if can? :destroy, comment %>
    <%= link_to 'Destroy Comment', [comment.article, comment],
                 method: :delete,
                 data: { confirm: 'Are you sure?' } %>
  <% end %>
  <hr/>